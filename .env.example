# Database (PostgreSQL)
DATABASE_URL="postgresql://optidept:ebrightoptidept2025@103.209.156.174:5433/ebright_hrfs?schema=public"

# NextAuth
# Set this to your actual deployed URL in production (e.g. http://103.209.156.174 or https://yourdomain.com)
NEXTAUTH_URL="http://localhost:3000"

# Generate a strong secret: run -> openssl rand -base64 32
NEXTAUTH_SECRET="replace-with-a-strong-random-secret"

# Optional: for biometric encryption (if used)
# ENCRYPTION_KEY="replace-with-a-32-character-key!!"